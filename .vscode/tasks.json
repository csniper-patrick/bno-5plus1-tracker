{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Dev Server",
      "type": "shell",
      "command": "if [ -f /.dockerenv ] || [ -n \"$REMOTE_CONTAINERS\" ]; then npm run dev; fi",
      "windows": {
        "command": "if (Test-Path '/.dockerenv') { npm run dev }"
      },
      "isBackground": true,
      "problemMatcher": [],
      "runOptions": {
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "silent",
        "panel": "dedicated"
      }
    }
  ]
}